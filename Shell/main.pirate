func test(int a, int b): void
{
    return a + b;
}

var first = 1;
var second = 2;

test(first, second);

for var i = 0 to 10
{
    test(first, i);
}

while first < 10
{
    first = test(first, second);
}